# Quant Daily Knowledge - 2026-03-15

## Today at a glance
- 将“回测不可用”问题推进到可执行修复阶段，要求用真实请求完成可用性验证。
- 明确验收方式：通过 `curl`（或等价请求工具）验证回测接口链路，而不是只看前端页面状态。
- 扩充基础静态数据：确认采用宽松模式，并补全 `a_share_symbols.txt` 的股票 symbol。

## Changes
- **验证策略升级**：从“代码实现”升级为“接口级实测 + 结果对比”。
- **数据底座修复**：将 symbol 完整性列为回测稳定性前置条件，避免因标的缺失导致回测失败或偏差。
- **沟通方式优化**：减少反复确认，倾向“确认后直接落地并给对比结果”的执行节奏。

## Decisions
- **决策 1**：回测可用性以请求链路验证为准（curl 优先）。
  - **原因**：可绕开前端展示噪音，直接检验服务真实行为。
  - **备选**：仅凭页面展示判断；**未采用**，误判风险高。
- **决策 2**：采用宽松模式并补齐 symbol 清单。
  - **原因**：降低输入约束导致的失败率，提升首次可跑通概率。
  - **备选**：维持严格模式；**未采用**，当前阶段不利于快速恢复可用性。

## Risks / Issues
- symbol 补全若不完整，会继续触发“策略正常但数据缺失”的隐性失败。
- 仅有接口成功不足以代表业务成功，仍需核对结果质量（交易明细、收益曲线合理性）。

## Next Actions (P0/P1)
- **P0**：提供至少 1 组可复现 curl 用例（请求体 + 响应摘要 + 关键字段校验）。
- **P0**：补齐并校验 `a_share_symbols.txt`，记录补全来源与覆盖范围。
- **P1**：产出“回测可用性 smoke test”脚本，便于后续回归复用。

## References
- inbox: `quant/inbox/2026-03-15/`
- static data: `a_share_symbols.txt`
- source signals: “通过 curl 测可用性”“宽松模式并补全 symbol”